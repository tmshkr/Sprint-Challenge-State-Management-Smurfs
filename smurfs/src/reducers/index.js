const initialState = {
  smurfs: []
};

const reducer = (state = initialState, action) => {
  const newState = { ...state };

  switch (action.type) {
    default:
      break;
  }
  return newState;
};

export default reducer;
